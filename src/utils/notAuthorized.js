// import createError from "../utils/createError.js";

// function isEnterprise({childreen}){
//     if (!req.isEnterprise) {
    
//     return (
//           <Navigate to="/login" state={{ from: location }} />
//       );
// }}